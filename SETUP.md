# Инструкция по установке и запуску AURA

## Установка

### 1. Установка зависимостей сервера

```bash
cd server
npm install
```

### 2. Создание базы данных

Запустите миграцию для создания таблиц и импорта продуктов:

```bash
npm run migrate
```

Вы должны увидеть:
```
✅ Migrations completed successfully
📦 Importing 24 products from data.js...
✅ Products imported successfully
```

## Запуск

### Запуск сервера

Из папки `server`:

```bash
npm run dev
```

Или для production:

```bash
npm start
```

Сервер запустится на порту 3001: http://localhost:3001

### Открытие сайта

После запуска сервера откройте в браузере:

- **Главная страница**: http://localhost:3001/views/index.html
- **Каталог**: http://localhost:3001/views/catalog.html
- **Корзина**: http://localhost:3001/views/cart.html
- **Админ-панель**: http://localhost:3001/admin/index.html

## Структура проекта

```
hw/
├── server/                  # Backend сервер
│   ├── database/           # База данных SQLite
│   │   ├── aura.db        # Файл базы данных
│   │   ├── db.js          # Подключение к БД
│   │   └── schema.sql     # Схема таблиц
│   ├── routes/            # API endpoints
│   │   ├── products.js    # GET, POST, PUT, DELETE /api/products
│   │   ├── orders.js      # GET, POST, PATCH, DELETE /api/orders
│   │   ├── customers.js   # GET, PUT, DELETE /api/customers
│   │   ├── banners.js     # GET, POST, PUT, DELETE /api/banners
│   │   ├── collections.js # GET, POST, PUT, DELETE /api/collections
│   │   ├── categories.js  # GET, POST, PUT, DELETE /api/categories
│   │   ├── promocodes.js  # GET, POST, PUT, DELETE /api/promocodes
│   │   ├── discounts.js   # GET, POST, PUT, DELETE /api/discounts
│   │   └── content.js     # GET, PUT /api/content
│   ├── migrations/        # Скрипты миграции БД
│   ├── server.js          # Главный файл сервера
│   ├── .env               # Конфигурация (порт, путь к БД)
│   └── package.json       # Зависимости Node.js
│
├── public/                 # Статические файлы
│   ├── css/               # Стили
│   ├── js/                # JavaScript
│   │   ├── api.js         # API клиент (все запросы к серверу)
│   │   ├── products-loader.js  # Загрузка продуктов из API
│   │   ├── banners-loader.js   # Загрузка баннеров из API
│   │   ├── app.js         # Общий функционал
│   │   ├── cart-page.js   # Логика корзины
│   │   ├── catalog-page.js     # Логика каталога
│   │   ├── product-page.js     # Логика страницы товара
│   │   └── data.js        # Fallback данные
│   └── images/            # Изображения
│
├── views/                  # HTML страницы клиента
│   ├── index.html         # Главная
│   ├── catalog.html       # Каталог товаров
│   ├── product.html       # Страница товара
│   └── cart.html          # Корзина и оформление
│
├── admin/                  # Админ-панель
│   ├── index.html         # Дашборд
│   ├── products.html      # Управление товарами
│   ├── orders.html        # Управление заказами
│   ├── customers.html     # Управление клиентами
│   ├── banners.html       # Управление баннерами
│   ├── collections.html   # Управление коллекциями
│   ├── categories.html    # Управление категориями
│   ├── promocodes.html    # Управление промокодами
│   ├── discounts.html     # Управление скидками
│   └── content.html       # Управление контентом
│
└── TODO.md                # Список задач
```

## API Endpoints

### Products
- `GET /api/products` - Получить все товары (поддерживает фильтры: category, tag, search, limit, offset)
- `GET /api/products/:id` - Получить товар по ID
- `POST /api/products` - Создать товар
- `PUT /api/products/:id` - Обновить товар
- `DELETE /api/products/:id` - Удалить товар

### Orders
- `GET /api/orders` - Получить все заказы (фильтр: status)
- `GET /api/orders/:id` - Получить заказ по ID
- `POST /api/orders` - Создать заказ
- `PATCH /api/orders/:id/status` - Обновить статус заказа
- `DELETE /api/orders/:id` - Удалить заказ

### Customers
- `GET /api/customers` - Получить всех клиентов
- `GET /api/customers/:id` - Получить клиента по ID (с заказами)
- `PUT /api/customers/:id` - Обновить клиента
- `DELETE /api/customers/:id` - Удалить клиента

### Banners
- `GET /api/banners` - Получить все баннеры
- `GET /api/banners/active` - Получить активные баннеры
- `GET /api/banners/:id` - Получить баннер по ID
- `POST /api/banners` - Создать баннер
- `PUT /api/banners/:id` - Обновить баннер
- `DELETE /api/banners/:id` - Удалить баннер

### Collections
- `GET /api/collections` - Получить все коллекции
- `GET /api/collections/slug/:slug` - Получить коллекцию по slug (с товарами)
- `GET /api/collections/:id` - Получить коллекцию по ID
- `POST /api/collections` - Создать коллекцию
- `PUT /api/collections/:id` - Обновить коллекцию
- `DELETE /api/collections/:id` - Удалить коллекцию

### Categories
- `GET /api/categories` - Получить все категории
- `GET /api/categories/visible` - Получить только видимые категории
- `POST /api/categories` - Создать категорию
- `PUT /api/categories/:id` - Обновить категорию
- `DELETE /api/categories/:id` - Удалить категорию

### Promocodes
- `GET /api/promocodes` - Получить все промокоды
- `POST /api/promocodes/validate` - Проверить промокод
- `POST /api/promocodes` - Создать промокод
- `PUT /api/promocodes/:id` - Обновить промокод
- `DELETE /api/promocodes/:id` - Удалить промокод

### Discounts
- `GET /api/discounts` - Получить все скидки
- `GET /api/discounts/active` - Получить активные скидки
- `POST /api/discounts` - Создать скидку
- `PUT /api/discounts/:id` - Обновить скидку
- `DELETE /api/discounts/:id` - Удалить скидку

### Content
- `GET /api/content` - Получить весь контент
- `GET /api/content/:key` - Получить контент по ключу
- `PUT /api/content/:key` - Обновить контент (создаёт если не существует)

## База данных

SQLite база данных находится в `server/database/aura.db` и содержит следующие таблицы:

1. **products** - Товары
2. **orders** - Заказы
3. **customers** - Клиенты
4. **banners** - Баннеры для главной страницы
5. **collections** - Коллекции товаров
6. **categories** - Категории товаров
7. **promocodes** - Промокоды
8. **discounts** - Скидки
9. **content_settings** - Настройки контента
10. **reviews** - Отзывы

## Что уже работает

✅ **Backend:**
- Express.js сервер на порту 3001
- SQLite база данных с 10 таблицами
- 9 REST API endpoints с полным CRUD
- Миграция данных (24 товара импортированы)

✅ **Frontend:**
- API клиент (`api.js`) для всех запросов
- Загрузчик товаров с fallback на статические данные
- Динамические баннеры на главной странице
- Главная страница загружает данные из API
- Каталог использует API для фильтрации и поиска
- Страница товара загружает из API
- Корзина создаёт заказы через API

## Что нужно доработать (TODO)

⏳ Админ-панель (пока использует LocalStorage):
- Обновить все страницы админки для использования API вместо REPO
- Тестирование CRUD операций через админку

⏳ Коллекции:
- Создать страницу collection.html для отображения коллекций

⏳ Скидки:
- Загружать активные скидки через API
- Применять скидки к ценам товаров
- Показывать бейджи "Скидка X%"

⏳ Миграция на PostgreSQL:
- Когда будет готово к production, перенести с SQLite на PostgreSQL

## Технологии

- **Backend**: Node.js, Express.js
- **Database**: SQLite (с возможностью миграции на PostgreSQL)
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **API**: RESTful JSON API

## Порты

- **Server**: 3001
- Если порт 3001 занят, измените в файле `server/.env`:
  ```
  PORT=3002
  ```

## Отладка

Если что-то не работает:

1. Проверьте, что сервер запущен: http://localhost:3001/api/products
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Проверьте логи сервера в терминале
4. Убедитесь, что миграция выполнена (`npm run migrate`)
5. Проверьте, что файл `server/database/aura.db` существует
