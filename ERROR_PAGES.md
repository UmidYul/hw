# Страницы ошибок Higher Waist

Красивые, минималистичные страницы ошибок для сайта Higher Waist.

## Созданные страницы

### 1. **404.html** - Страница не найдена
- Используется когда запрашиваемая страница не существует
- Путь: `/views/404.html`
- Включает ссылки на главную страницу и каталог

### 2. **error.html** - Универсальная страница ошибок сервера
- Поддерживает различные коды ошибок: 400, 401, 403, 500, 502, 503, 504
- Путь: `/views/error.html`
- Автоматически определяет код ошибки из параметра URL `?code=500`
- Динамически меняет заголовок и описание в зависимости от кода

### 3. **maintenance.html** - Техническое обслуживание
- Используется во время планового обслуживания сайта
- Путь: `/views/maintenance.html`
- Показывает статус и примерное время завершения работ
- Включает контактную информацию и социальные сети

## Интеграция с сервером

### Для Express.js

```javascript
// В файле server.js или server/server.js

const path = require('path');
const express = require('express');
const app = express();

// Обработка 404 ошибок
app.use((req, res, next) => {
    res.status(404).sendFile(path.join(__dirname, 'views', '404.html'));
});

// Обработка ошибок сервера
app.use((err, req, res, next) => {
    console.error(err.stack);
    const errorCode = err.status || 500;
    res.status(errorCode).sendFile(
        path.join(__dirname, 'views', 'error.html')
    );
});
```

### Примеры использования

#### 404 - Страница не найдена
```javascript
app.get('/несуществующая-страница', (req, res) => {
    res.status(404).sendFile(path.join(__dirname, 'views', '404.html'));
});
```

#### 500 - Ошибка сервера
```javascript
app.get('/проблемный-роут', (req, res) => {
    res.status(500).sendFile(path.join(__dirname, 'views', 'error.html'));
});
```

#### Другие коды ошибок
```javascript
// 403 Доступ запрещён
res.status(403).redirect('/error?code=403');

// 503 Сервис недоступен
res.status(503).redirect('/error?code=503');
```

#### Техническое обслуживание
```javascript
// Временное перенаправление всех запросов
app.use((req, res) => {
    res.status(503).sendFile(path.join(__dirname, 'views', 'maintenance.html'));
});
```

## Дизайн и функции

### Общие особенности:
- ✨ Минималистичный дизайн в стиле Higher Waist
- 📱 Адаптивный дизайн для всех устройств
- 🎨 Использует фирменные шрифты (Cormorant Garamond, Inter)
- ⚡ Плавные анимации при загрузке
- 🎯 Интерактивные элементы

### Цветовая схема:
- Фон: `#FAFAFA`
- Текст: `#2D2D2D`
- Акцент: `#C9A26C` (золотой)
- Вторичный текст: `#757575`

### Кнопки и действия:
- Возврат на главную страницу
- Обновление страницы (для серверных ошибок)
- Переход в каталог
- Контактная информация

## Кастомизация

### Изменение времени обслуживания
В файле `maintenance.html` найдите:
```javascript
const estimates = ['~2 часа', '~1.5 часа', '~1 час', '~30 минут', 'Завершается...'];
```

### Добавление новых кодов ошибок
В файле `error.html` добавьте в объект `errorMessages`:
```javascript
'501': {
    title: 'Не реализовано',
    description: 'Сервер не поддерживает функциональность...'
}
```

### Изменение контактов
В `maintenance.html`:
```html
<div class="contact-info">
    По всем вопросам: <a href="mailto:ваш@email.com">ваш@email.com</a>
</div>
```

## Тестирование

Откройте страницы в браузере для тестирования:
- `/views/404.html` - страница 404
- `/views/error.html` - страница ошибки 500 (по умолчанию)
- `/views/error.html?code=403` - страница ошибки 403
- `/views/maintenance.html` - страница обслуживания

## SEO и метатеги

Все страницы включают базовые метатеги:
- `<title>` - уникальный для каждой страницы
- `<meta name="description">` - описание страницы
- Адаптивный viewport для мобильных устройств

## Поддержка браузеров

- ✅ Chrome, Edge, Firefox, Safari (последние версии)
- ✅ Мобильные браузеры iOS и Android
- ✅ IE11+ (с базовой поддержкой)
